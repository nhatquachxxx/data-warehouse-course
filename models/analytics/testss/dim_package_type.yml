version: 2

models:
  - name: dim_package_type
    columns:
      - name: package_type_key
        tests:
          - not_null
          - unique

      - name: package_type_name
        tests:
          - not_null
